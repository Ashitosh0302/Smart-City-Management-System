<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Emergency Alert Registered - Police Response</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link
        href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&family=Noto+Sans:wght@400;500;600&display=swap"
        rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Noto Sans', sans-serif;
            background: linear-gradient(135deg, #f0f8ff 0%, #e6f2ff 100%);
            color: #333;
            line-height: 1.6;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .alert-container {
            max-width: 800px;
            width: 100%;
            background: white;
            border-radius: 20px;
            box-shadow: 0 15px 40px rgba(0, 30, 80, 0.15);
            overflow: hidden;
            animation: containerAppear 0.8s ease-out;
        }

        /* Header Section */
        .alert-header {
            background: linear-gradient(135deg, #003366 0%, #004080 100%);
            padding: 40px;
            text-align: center;
            color: white;
            position: relative;
            overflow: hidden;
        }

        .alert-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><text x="50" y="50" font-size="8" fill="rgba(255,255,255,0.03)" text-anchor="middle" dominant-baseline="middle">सत्यमेव जयते</text></svg>');
            background-size: 150px;
            opacity: 0.1;
        }

        .alert-icon {
            width: 120px;
            height: 120px;
            background: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 30px;
            animation: iconAppear 1s ease-out 0.3s both;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            border: 4px solid #28a745;
        }

        .alert-icon i {
            font-size: 5rem;
            color: #28a745;
            animation: iconPulse 2s infinite;
        }

        .alert-header h1 {
            font-family: 'Montserrat', sans-serif;
            font-size: 2.8rem;
            margin-bottom: 15px;
            animation: slideDown 1s ease-out 0.5s both;
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
        }

        .alert-subtitle {
            font-size: 1.5rem;
            color: #90ee90;
            font-weight: 600;
            margin-bottom: 20px;
            animation: fadeInUp 1s ease-out 0.7s both;
        }

        .alert-message {
            font-size: 1.2rem;
            opacity: 0.9;
            max-width: 600px;
            margin: 0 auto;
            animation: fadeInUp 1s ease-out 0.9s both;
        }

        /* Status Section */
        .status-section {
            padding: 40px;
            text-align: center;
            background: #f8fff9;
        }

        .status-title {
            font-family: 'Montserrat', sans-serif;
            color: #155724;
            font-size: 2rem;
            margin-bottom: 30px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px;
            animation: fadeInUp 1s ease-out 1.1s both;
        }

        .status-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 25px;
            margin-bottom: 40px;
        }

        .status-card {
            background: white;
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.08);
            border-top: 4px solid #003366;
            transition: transform 0.3s ease;
            animation: fadeInUp 0.8s ease-out;
            position: relative;
            overflow: hidden;
        }

        .status-card:nth-child(2) {
            border-top-color: #28a745;
            animation-delay: 0.1s;
        }

        .status-card:nth-child(3) {
            border-top-color: #ffc107;
            animation-delay: 0.2s;
        }

        .status-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.12);
        }

        .card-icon {
            width: 60px;
            height: 60px;
            background: #f8f9fa;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            color: #003366;
            font-size: 1.8rem;
        }

        .card-title {
            font-family: 'Montserrat', sans-serif;
            color: #003366;
            margin-bottom: 10px;
            font-size: 1.3rem;
            font-weight: 600;
        }

        .card-value {
            color: #28a745;
            font-weight: 700;
            font-size: 1.4rem;
            margin-bottom: 10px;
        }

        .card-desc {
            color: #6c757d;
            font-size: 0.95rem;
        }

        /* Response Section */
        .response-section {
            padding: 40px;
            background: white;
        }

        .response-title {
            font-family: 'Montserrat', sans-serif;
            color: #003366;
            font-size: 1.8rem;
            margin-bottom: 30px;
            text-align: center;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px;
        }

        .response-steps {
            max-width: 600px;
            margin: 0 auto 40px;
            position: relative;
        }

        .step {
            display: flex;
            align-items: center;
            margin-bottom: 40px;
            position: relative;
            animation: fadeInRight 0.8s ease-out;
        }

        .step:nth-child(2) {
            animation-delay: 0.2s;
        }

        .step:nth-child(3) {
            animation-delay: 0.4s;
        }

        .step:last-child {
            margin-bottom: 0;
        }

        .step-number {
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, #003366, #004080);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 700;
            font-size: 1.5rem;
            margin-right: 25px;
            flex-shrink: 0;
            z-index: 2;
            position: relative;
        }

        .step-content {
            flex: 1;
            background: #f8f9fa;
            padding: 25px;
            border-radius: 12px;
            border-left: 4px solid #28a745;
        }

        .step-title {
            font-family: 'Montserrat', sans-serif;
            color: #003366;
            margin-bottom: 10px;
            font-size: 1.2rem;
            font-weight: 600;
        }

        .step-desc {
            color: #5a6c7d;
            line-height: 1.7;
        }

        /* Action Section */
        .action-section {
            padding: 40px;
            background: linear-gradient(135deg, #e8f5e9 0%, #d4edda 100%);
            text-align: center;
            border-top: 1px solid #c3e6cb;
        }

        .action-title {
            font-family: 'Montserrat', sans-serif;
            color: #155724;
            font-size: 1.8rem;
            margin-bottom: 25px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px;
        }

        .action-buttons {
            display: flex;
            gap: 20px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .action-btn {
            padding: 15px 35px;
            font-size: 1.1rem;
            font-weight: 600;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 12px;
            font-family: 'Montserrat', sans-serif;
        }

        .btn-primary {
            background: linear-gradient(135deg, #003366, #004080);
            color: white;
        }

        .btn-secondary {
            background: white;
            color: #003366;
            border: 2px solid #003366;
        }

        .action-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.15);
        }

        /* Footer */
        .alert-footer {
            padding: 30px;
            background: #003366;
            color: white;
            text-align: center;
            font-size: 0.9rem;
        }

        .footer-logo {
            font-family: 'Montserrat', sans-serif;
            font-size: 1.3rem;
            margin-bottom: 15px;
            font-weight: 700;
            color: #ffcc00;
        }

        .footer-motto {
            font-style: italic;
            margin-bottom: 15px;
            color: rgba(255, 255, 255, 0.9);
        }

        /* Animations */
        @keyframes containerAppear {
            0% {
                opacity: 0;
                transform: scale(0.95) translateY(20px);
            }
            100% {
                opacity: 1;
                transform: scale(1) translateY(0);
            }
        }

        @keyframes iconAppear {
            0% {
                opacity: 0;
                transform: scale(0) rotate(-180deg);
            }
            70% {
                opacity: 1;
                transform: scale(1.1) rotate(10deg);
            }
            100% {
                transform: scale(1) rotate(0);
            }
        }

        @keyframes iconPulse {
            0%, 100% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.1);
            }
        }

        @keyframes slideDown {
            0% {
                opacity: 0;
                transform: translateY(-30px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeInUp {
            0% {
                opacity: 0;
                transform: translateY(30px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeInRight {
            0% {
                opacity: 0;
                transform: translateX(-30px);
            }
            100% {
                opacity: 1;
                transform: translateX(0);
            }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .alert-header h1 {
                font-size: 2.2rem;
            }
            
            .alert-subtitle {
                font-size: 1.3rem;
            }
            
            .alert-icon {
                width: 100px;
                height: 100px;
            }
            
            .alert-icon i {
                font-size: 4rem;
            }
            
            .status-title, .response-title, .action-title {
                font-size: 1.5rem;
                flex-direction: column;
                gap: 10px;
                text-align: center;
            }
            
            .status-grid {
                grid-template-columns: 1fr;
                gap: 20px;
            }
            
            .step {
                flex-direction: column;
                align-items: flex-start;
            }
            
            .step-number {
                margin-right: 0;
                margin-bottom: 15px;
            }
            
            .action-buttons {
                flex-direction: column;
                align-items: center;
            }
            
            .action-btn {
                width: 100%;
                max-width: 300px;
                justify-content: center;
            }
        }

        @media (max-width: 480px) {
            .alert-header, .status-section, .response-section, .action-section {
                padding: 30px 25px;
            }
            
            .alert-header h1 {
                font-size: 1.8rem;
            }
            
            .alert-subtitle {
                font-size: 1.1rem;
            }
            
            .alert-message {
                font-size: 1rem;
            }
        }
    </style>
</head>

<body>
    <div class="alert-container">
        <!-- Header Section -->
        <div class="alert-header">
            <div class="alert-icon">
                <i class="fas fa-shield-check"></i>
            </div>
            <h1>EMERGENCY ALERT REGISTERED</h1>
            <div class="alert-subtitle">We'll Take Quick Action!</div>
            <p class="alert-message">
                Your emergency alert has been successfully received. Our police response team has been activated and is on the way to your location.
            </p>
        </div>

        <!-- Status Section -->
        <div class="status-section">
            <h2 class="status-title">
                <i class="fas fa-info-circle"></i>
                Alert Information
            </h2>
            
            <div class="status-grid">
                <div class="status-card">
                    <div class="card-icon">
                        <i class="fas fa-fingerprint"></i>
                    </div>
                    <div class="card-title">Alert ID</div>
                    <div class="card-value" id="alertId">ALERT-8492X7</div>
                    <div class="card-desc">Unique reference number</div>
                </div>
                
                <div class="status-card">
                    <div class="card-icon">
                        <i class="fas fa-clock"></i>
                    </div>
                    <div class="card-title">Response Time</div>
                    <div class="card-value" id="responseTime">5-8 MINUTES</div>
                    <div class="card-desc">Estimated arrival</div>
                </div>
                
                <div class="status-card">
                    <div class="card-icon">
                        <i class="fas fa-user-shield"></i>
                    </div>
                    <div class="card-title">Response Team</div>
                    <div class="card-value" id="teamStatus">DEPLOYED</div>
                    <div class="card-desc">Patrol unit dispatched</div>
                </div>
            </div>
        </div>

        <!-- Response Section -->
        <div class="response-section">
            <h2 class="response-title">
                <i class="fas fa-shipping-fast"></i>
                What Happens Next
            </h2>
            
            <div class="response-steps">
                <div class="step">
                    <div class="step-number">1</div>
                    <div class="step-content">
                        <div class="step-title">Alert Received & Verified</div>
                        <div class="step-desc">Your emergency alert has been received at our command center and verified by our operators.</div>
                    </div>
                </div>
                
                <div class="step">
                    <div class="step-number">2</div>
                    <div class="step-content">
                        <div class="step-title">Nearest Patrol Dispatched</div>
                        <div class="step-desc">The closest available police patrol has been immediately dispatched to your location.</div>
                    </div>
                </div>
                
                <div class="step">
                    <div class="step-number">3</div>
                    <div class="step-content">
                        <div class="step-title">On-Site Assistance</div>
                        <div class="step-desc">Trained police officers will arrive at your location to provide immediate assistance and support.</div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Action Section -->
        <div class="action-section">
            <h2 class="action-title">
                <i class="fas fa-exclamation-circle"></i>
                Important Instructions
            </h2>
            
            <div class="action-buttons">
                <button class="action-btn btn-primary" onclick="updateStatus()">
                    <i class="fas fa-sync-alt"></i>
                    Update Status
                </button>
                <button class="action-btn btn-secondary" onclick="callEmergency()">
                    <i class="fas fa-phone-alt"></i>
                    Call Emergency (100)
                </button>
                <button class="action-btn btn-secondary" onclick="goBack()">
                    <i class="fas fa-home"></i>
                    Return to Home
                </button>
            </div>
            
            <div style="margin-top: 30px; padding: 20px; background: white; border-radius: 8px; max-width: 600px; margin-left: auto; margin-right: auto;">
                <h4 style="color: #003366; margin-bottom: 15px; display: flex; align-items: center; gap: 10px; justify-content: center;">
                    <i class="fas fa-lightbulb"></i>
                    Safety Tips While Waiting
                </h4>
                <p style="color: #5a6c7d; font-size: 0.95rem; line-height: 1.6;">
                    • Stay in a safe location if possible<br>
                    • Keep your phone accessible and charged<br>
                    • If it's safe to do so, stay near the main entrance<br>
                    • Do not approach any potential danger<br>
                    • Follow instructions from arriving officers
                </p>
            </div>
        </div>

        <!-- Footer -->
        <div class="alert-footer">
            <div class="footer-logo">POLICE EMERGENCY SERVICES</div>
            <div class="footer-motto">सत्यमेव जयते - Truth Alone Triumphs</div>
            <p>24/7 Emergency Response | Your Safety is Our Priority</p>
            <p style="margin-top: 15px; opacity: 0.8; font-size: 0.85rem;">
                <i class="fas fa-shield-alt"></i> Your data is protected and used only for emergency response purposes.
            </p>
        </div>
    </div>

    <script>
        // Generate random alert ID
        function generateAlertId() {
            const letters = 'ABCDEFGHJKLMNPQRSTUVWXYZ';
            const numbers = '0123456789';
            let id = 'ALERT-';
            
            for (let i = 0; i < 6; i++) {
                if (i < 4) {
                    id += numbers.charAt(Math.floor(Math.random() * numbers.length));
                } else {
                    id += letters.charAt(Math.floor(Math.random() * letters.length));
                }
            }
            return id;
        }

        // Update response time dynamically
        function updateResponseTime() {
            const times = ['5-8 MINUTES', '4-7 MINUTES', '6-9 MINUTES', '5-10 MINUTES'];
            const randomTime = times[Math.floor(Math.random() * times.length)];
            document.getElementById('responseTime').textContent = randomTime;
        }

        // Update status function
        function updateStatus() {
            const alertId = document.getElementById('alertId');
            const teamStatus = document.getElementById('teamStatus');
            
            // Add loading animation
            teamStatus.innerHTML = '<i class="fas fa-spinner fa-spin"></i> UPDATING';
            
            setTimeout(() => {
                const statuses = ['EN ROUTE', 'ARRIVING SOON', 'NEARBY', 'APPROACHING'];
                const randomStatus = statuses[Math.floor(Math.random() * statuses.length)];
                teamStatus.textContent = randomStatus;
                teamStatus.style.color = '#28a745';
                
                // Update response time
                updateResponseTime();
                
                // Show success message
                showToast('Status updated successfully!');
            }, 1500);
        }

        // Call emergency function
        function callEmergency() {
            showToast('Dialing emergency number 100...');
            // In a real app, this would initiate a phone call
            setTimeout(() => {
                showToast('Connecting to emergency services...');
            }, 1000);
        }

        // Go back function
        function goBack() {
            showToast('Returning to home page...');
            setTimeout(() => {
                // In a real app, this would redirect to home page
                window.location.href = 'emergency-form.html';
            }, 1000);
        }

        // Toast notification function
        function showToast(message) {
            // Remove existing toast
            const existingToast = document.querySelector('.toast');
            if (existingToast) existingToast.remove();
            
            // Create new toast
            const toast = document.createElement('div');
            toast.className = 'toast';
            toast.innerHTML = `
                <div style="
                    position: fixed;
                    top: 20px;
                    right: 20px;
                    background: #003366;
                    color: white;
                    padding: 15px 25px;
                    border-radius: 8px;
                    box-shadow: 0 5px 20px rgba(0,0,0,0.2);
                    z-index: 1000;
                    animation: toastSlide 0.3s ease-out;
                    display: flex;
                    align-items: center;
                    gap: 10px;
                    max-width: 300px;
                ">
                    <i class="fas fa-info-circle"></i>
                    <span>${message}</span>
                </div>
            `;
            
            document.body.appendChild(toast);
            
            // Remove toast after 3 seconds
            setTimeout(() => {
                toast.style.animation = 'toastSlideOut 0.3s ease-out forwards';
                setTimeout(() => toast.remove(), 300);
            }, 3000);
        }

        // Initialize page
        document.addEventListener('DOMContentLoaded', function() {
            // Set alert ID
            document.getElementById('alertId').textContent = generateAlertId();
            
            // Set initial response time
            updateResponseTime();
            
            // Add CSS for toast animation
            const style = document.createElement('style');
            style.textContent = `
                @keyframes toastSlide {
                    from { transform: translateX(100%); opacity: 0; }
                    to { transform: translateX(0); opacity: 1; }
                }
                @keyframes toastSlideOut {
                    from { transform: translateX(0); opacity: 1; }
                    to { transform: translateX(100%); opacity: 0; }
                }
            `;
            document.head.appendChild(style);
        });
    </script>
</body>

</html>