<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Appointment Confirmed</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        
        .success-card {
            background: white;
            border-radius: 24px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 500px;
            padding: 50px 40px;
            text-align: center;
            position: relative;
            overflow: hidden;
            animation: cardAppear 0.6s ease-out;
        }
        
        @keyframes cardAppear {
            0% { 
                opacity: 0;
                transform: translateY(30px) scale(0.95);
            }
            100% { 
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }
        
        .success-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 6px;
            background: linear-gradient(90deg, #00c853, #64dd17, #aeea00);
        }
        
        .success-icon {
            width: 100px;
            height: 100px;
            background: linear-gradient(135deg, #00c853, #64dd17);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 30px;
            color: white;
            font-size: 48px;
            animation: iconPop 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            box-shadow: 0 10px 30px rgba(0, 200, 83, 0.3);
        }
        
        @keyframes iconPop {
            0% { 
                transform: scale(0);
                opacity: 0;
            }
            70% { 
                transform: scale(1.1);
            }
            100% { 
                transform: scale(1);
                opacity: 1;
            }
        }
        
        .success-title {
            font-size: 2.5rem;
            font-weight: 700;
            color: #1a237e;
            margin-bottom: 15px;
            line-height: 1.2;
        }
        
        .success-message {
            font-size: 1.2rem;
            color: #5a5a5a;
            margin-bottom: 40px;
            line-height: 1.6;
        }
        
        .confirmation-number {
            background: #f1f8e9;
            border: 2px dashed #c8e6c9;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 35px;
            text-align: center;
        }
        
        .confirmation-label {
            font-size: 0.95rem;
            color: #689f38;
            font-weight: 600;
            margin-bottom: 8px;
            display: block;
        }
        
        .confirmation-id {
            font-size: 1.8rem;
            font-weight: 700;
            color: #2e7d32;
            letter-spacing: 1px;
            font-family: 'Courier New', monospace;
        }
        
        .next-steps {
            background: #f8f9fa;
            border-radius: 16px;
            padding: 25px;
            margin-bottom: 35px;
            text-align: left;
        }
        
        .next-steps-title {
            font-size: 1.1rem;
            color: #1a237e;
            font-weight: 600;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .next-steps-title i {
            color: #00c853;
        }
        
        .next-steps-list {
            list-style-type: none;
        }
        
        .next-steps-list li {
            padding: 10px 0;
            color: #5a5a5a;
            display: flex;
            align-items: flex-start;
            gap: 12px;
        }
        
        .next-steps-list li i {
            color: #00c853;
            margin-top: 3px;
            font-size: 0.9rem;
        }
        
        .action-buttons {
            display: flex;
            flex-direction: column;
            gap: 15px;
            margin-top: 30px;
        }
        
        .btn {
            padding: 18px 24px;
            border-radius: 12px;
            font-size: 1.05rem;
            font-weight: 600;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            transition: all 0.3s ease;
            border: none;
            text-decoration: none;
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #00c853, #64dd17);
            color: white;
            box-shadow: 0 6px 20px rgba(0, 200, 83, 0.3);
        }
        
        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(0, 200, 83, 0.4);
        }
        
        .btn-secondary {
            background: white;
            color: #5a5a5a;
            border: 2px solid #e0e0e0;
        }
        
        .btn-secondary:hover {
            background: #f5f5f5;
            transform: translateY(-3px);
            border-color: #d0d0d0;
        }
        
        .success-footer {
            margin-top: 30px;
            font-size: 0.9rem;
            color: #9e9e9e;
            line-height: 1.5;
        }
        
        .success-footer a {
            color: #00c853;
            text-decoration: none;
            font-weight: 600;
        }
        
        .success-footer a:hover {
            text-decoration: underline;
        }
        
        .pulse {
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0% { box-shadow: 0 0 0 0 rgba(0, 200, 83, 0.4); }
            70% { box-shadow: 0 0 0 15px rgba(0, 200, 83, 0); }
            100% { box-shadow: 0 0 0 0 rgba(0, 200, 83, 0); }
        }
        
        @media (max-width: 600px) {
            .success-card {
                padding: 40px 25px;
                border-radius: 20px;
            }
            
            .success-title {
                font-size: 2rem;
            }
            
            .success-message {
                font-size: 1.1rem;
            }
            
            .confirmation-id {
                font-size: 1.5rem;
            }
            
            .btn {
                padding: 16px 20px;
            }
        }
        
        @media (max-width: 400px) {
            .success-card {
                padding: 35px 20px;
            }
            
            .success-title {
                font-size: 1.8rem;
            }
        }
    </style>
</head>
<body>
    <div class="success-card">
        <div class="success-icon pulse">
            <i class="fas fa-check"></i>
        </div>
        
        <h1 class="success-title">Appointment Successfully Registered</h1>
        
        <p class="success-message">
            Your appointment has been confirmed and scheduled. 
            We've sent a confirmation email with all the details.
        </p>
        
        <div class="confirmation-number">
            <span class="confirmation-label">CONFIRMATION NUMBER</span>
            <div class="confirmation-id">APT-4892-16</div>
        </div>
        
        <div class="next-steps">
            <div class="next-steps-title">
                <i class="fas fa-info-circle"></i>
                <span>What happens next?</span>
            </div>
            <ul class="next-steps-list">
                <li>
                    <i class="fas fa-envelope"></i>
                    <span>Check your email for confirmation details</span>
                </li>
                <li>
                    <i class="fas fa-bell"></i>
                    <span>You'll receive a reminder 24 hours before your appointment</span>
                </li>
                <li>
                    <i class="fas fa-clock"></i>
                    <span>Please arrive 10 minutes before your scheduled time</span>
                </li>
            </ul>
        </div>
        
        <div class="action-buttons">
            <button class="btn btn-primary" id="calendarBtn">
                <i class="fas fa-calendar-plus"></i>
                <span>Add to Calendar</span>
            </button>
            
            <button class="btn btn-secondary" id="homeBtn">
                <i class="fas fa-home"></i>
                <span>Return to Homepage</span>
            </button>
        </div>
        
        <div class="success-footer">
            <p>Need to make changes? <a href="#">Reschedule or cancel</a> up to 24 hours before your appointment.</p>
            <p style="margin-top: 10px;">Questions? Contact support@example.com</p>
        </div>
    </div>

    <script>
        // Button interactions
        document.getElementById('calendarBtn').addEventListener('click', function() {
            const originalText = this.innerHTML;
            this.innerHTML = '<i class="fas fa-check"></i><span>Added to Calendar</span>';
            this.style.background = 'linear-gradient(135deg, #2e7d32, #4caf50)';
            this.disabled = true;
            
            // Show a subtle confirmation
            const icon = document.querySelector('.success-icon');
            icon.classList.remove('pulse');
            icon.style.transform = 'scale(1.1)';
            
            setTimeout(() => {
                icon.style.transform = 'scale(1)';
                icon.classList.add('pulse');
            }, 300);
            
            // Reset after 3 seconds
            setTimeout(() => {
                this.innerHTML = originalText;
                this.style.background = 'linear-gradient(135deg, #00c853, #64dd17)';
                this.disabled = false;
            }, 3000);
        });
        
        document.getElementById('homeBtn').addEventListener('click', function() {
            this.innerHTML = '<i class="fas fa-spinner fa-spin"></i><span>Redirecting...</span>';
            this.disabled = true;
            
            // Simulate redirect
            setTimeout(() => {
                alert('Thank you! Redirecting to homepage...');
                this.innerHTML = '<i class="fas fa-home"></i><span>Return to Homepage</span>';
                this.disabled = false;
            }, 1500);
        });
        
        // Add some visual feedback when the page loads
        window.addEventListener('load', function() {
            const title = document.querySelector('.success-title');
            title.style.opacity = '0';
            title.style.transform = 'translateY(10px)';
            
            setTimeout(() => {
                title.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
                title.style.opacity = '1';
                title.style.transform = 'translateY(0)';
            }, 300);
            
            // Animate list items one by one
            const listItems = document.querySelectorAll('.next-steps-list li');
            listItems.forEach((item, index) => {
                item.style.opacity = '0';
                item.style.transform = 'translateX(-10px)';
                
                setTimeout(() => {
                    item.style.transition = 'opacity 0.4s ease, transform 0.4s ease';
                    item.style.opacity = '1';
                    item.style.transform = 'translateX(0)';
                }, 600 + (index * 150));
            });
        });
    </script>
</body>
</html>