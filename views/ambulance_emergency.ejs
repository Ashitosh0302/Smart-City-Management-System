<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Emergency Ambulance Services</title>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600&display=swap" rel="stylesheet">
<style>
    * { margin:0; padding:0; box-sizing:border-box; font-family:'Inter',sans-serif; }
    body { background:#f0f4f8; color:#334155; line-height:1.6; min-height:100vh; overflow-x: hidden; }

    .container { max-width:1000px; margin:0 auto; padding:30px 20px; }

    /* HEADER - UPDATED COLOR SCHEME */
    .header {
        position: relative;
        height:280px;
        border-radius:20px;
        overflow:hidden;
        margin-bottom:50px;
        display:flex;
        align-items:center;
        justify-content:center;
        background: linear-gradient(135deg, #2563eb, #3b82f6, #60a5fa);
        background-size: 300% 300%;
        animation: gradientShift 8s ease infinite alternate, float 6s ease-in-out infinite;
        box-shadow:0 15px 35px rgba(37, 99, 235, 0.2);
        transform: translateY(0);
        transition: transform 0.5s ease;
    }

    .header:hover {
        transform: translateY(-5px);
        box-shadow:0 20px 40px rgba(37, 99, 235, 0.3);
    }

    .header-content {
        position: relative;
        z-index:2;
        text-align:center;
        padding:40px;
        color:white;
        animation: fadeZoom 1.5s ease forwards, glow 3s ease-in-out infinite alternate;
    }

    .header-content h1 {
        font-family:'Playfair Display', serif;
        font-size:3rem;
        margin-bottom:15px;
        font-weight:700;
        letter-spacing:-0.5px;
        text-shadow:2px 2px 15px rgba(0,0,0,0.3);
        background: linear-gradient(to right, #ffffff, #dbeafe);
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
    }

    .header-tagline {
        font-size:1.3rem;
        font-weight:500;
        opacity:0;
        animation: fadeUp 1.8s ease forwards, textShimmer 3s infinite;
        animation-delay:0.5s;
        padding: 10px 20px;
        background: rgba(255,255,255,0.1);
        border-radius: 50px;
        backdrop-filter: blur(5px);
        display: inline-block;
    }

    /* EMERGENCY CARD - UPDATED COLORS */
    .emergency-card {
        background:white;
        border-radius:16px;
        padding:40px;
        margin-bottom:40px;
        box-shadow:0 10px 30px rgba(0,0,0,0.08);
        border:1px solid #e2e8f0;
        animation: fadeUp 1s ease forwards, cardFloat 4s ease-in-out infinite;
        position: relative;
        overflow: hidden;
        transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    }

    .emergency-card:hover {
        transform: translateY(-8px);
        box-shadow: 0 20px 40px rgba(37, 99, 235, 0.15);
    }

    .emergency-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 5px;
        background: linear-gradient(to right, #2563eb, #60a5fa);
    }

    .section-title {
        font-family:'Playfair Display', serif;
        font-size:1.8rem;
        color:#1e293b;
        margin-bottom:25px;
        position:relative;
        padding-bottom:15px;
    }

    .section-title::after {
        content:'';
        position:absolute;
        bottom:0; left:0; width:60px; height:3px;
        background: linear-gradient(to right, #2563eb, #60a5fa);
        border-radius:2px;
        animation: widthGrow 2s ease infinite alternate;
    }

    .emergency-number-container { 
        display:flex; 
        align-items:center; 
        gap:25px; 
        margin-bottom:25px; 
    }
    
    .number-icon { 
        width:70px; 
        height:70px; 
        background:linear-gradient(135deg,#ef4444, #dc2626); 
        border-radius:50%; 
        display:flex; 
        align-items:center; 
        justify-content:center; 
        box-shadow:0 8px 25px rgba(239, 68, 68, 0.3); 
        animation: gentlePulse 1.5s ease-in-out infinite, rotate 10s linear infinite; 
        transition: all 0.3s ease;
    }
    
    .number-icon:hover {
        transform: scale(1.1) rotate(180deg);
        animation: none;
    }
    
    .number-icon i { 
        font-size:1.8rem; 
        color:white; 
    }
    
    .emergency-number { 
        font-size:2.2rem; 
        font-weight:700; 
        color:#1e293b; 
        letter-spacing:1px; 
    }
    
    .emergency-number span { 
        background: linear-gradient(135deg,#ef4444, #dc2626); 
        -webkit-background-clip: text; 
        -webkit-text-fill-color: transparent; 
        font-weight:800; 
        text-shadow: 0 2px 10px rgba(239, 68, 68, 0.2);
    }

    .contact-info { 
        color:#475569; 
        font-size:1.1rem; 
        line-height:1.7; 
        padding:20px; 
        background:#f8fafc; 
        border-radius:12px; 
        border-left:4px solid #2563eb; 
        margin-top:20px; 
        transition:0.4s ease; 
    }
    
    .contact-info:hover { 
        transform:translateY(-3px) scale(1.01); 
        box-shadow:0 8px 20px rgba(37, 99, 235, 0.15); 
        background: white;
        border-left:4px solid #ef4444;
    }

    /* FORM CARD - UPDATED COLORS */
    .form-card {
        background:white;
        border-radius:16px;
        padding:40px;
        box-shadow:0 10px 30px rgba(0,0,0,0.08);
        border:1px solid #e2e8f0;
        animation: fadeUp 1.2s ease forwards, cardFloat 4s ease-in-out infinite alternate-reverse;
        transition: all 0.4s ease;
    }

    .form-card:hover {
        box-shadow: 0 15px 35px rgba(37, 99, 235, 0.1);
    }

    .form-group { 
        margin-bottom:30px; 
    }
    
    .form-label { 
        display:flex; 
        align-items:center; 
        gap:10px; 
        margin-bottom:12px; 
        color:#475569; 
        font-weight:500; 
        font-size:1.1rem; 
    }
    
    .form-label i { 
        color:#2563eb; 
        animation: iconBounce 2s infinite;
    }
    
    .address-input { 
        width:100%; 
        padding:18px; 
        border:2px solid #e2e8f0; 
        border-radius:12px; 
        font-size:1rem; 
        min-height:150px; 
        resize:vertical; 
        background:#f8fafc; 
        color:#334155; 
        transition:all 0.4s ease; 
    }
    
    .address-input:focus { 
        outline:none; 
        border-color:#2563eb; 
        background:white; 
        box-shadow:0 0 0 3px rgba(37, 99, 235, 0.1); 
        transform: translateY(-2px);
    }

    .submit-btn {
        background: linear-gradient(135deg,#2563eb, #3b82f6);
        color:white;
        border:none;
        padding:16px 45px;
        font-size:1.2rem;
        font-weight:600;
        border-radius:12px;
        cursor:pointer;
        display:inline-flex;
        align-items:center;
        gap:15px;
        transition:all 0.4s ease;
        animation: fadeUp 2s ease forwards, buttonPulse 3s infinite;
        position: relative;
        overflow: hidden;
    }
    
    .submit-btn::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
        transition: left 0.7s;
    }
    
    .submit-btn:hover::before {
        left: 100%;
    }
    
    .submit-btn:hover { 
        transform:scale(1.05) translateY(-3px); 
        box-shadow:0 10px 20px rgba(37, 99, 235, 0.3); 
        letter-spacing: 0.5px;
    }

    /* SUCCESS MESSAGE - UPDATED COLORS */
    .success-message { 
        background: linear-gradient(135deg,#d1fae5, #a7f3d0); 
        color:#065f46; 
        padding:30px; 
        border-radius:12px; 
        margin-top:30px; 
        display:none; 
        border:1px solid #10b981; 
        animation: successPop 0.8s ease forwards;
        transform-origin: center;
    }
    
    .success-message.show { 
        display:block; 
        animation: successPop 0.8s ease forwards, gentlePulse 3s infinite;
    }

    /* FOOTER - UPDATED COLORS */
    .footer { 
        text-align:center; 
        padding:50px 20px 30px; 
        color:#94a3b8; 
        margin-top:60px; 
        border-top:1px solid #e2e8f0; 
        animation: fadeUp 2.5s ease forwards;
    }
    
    .footer-logo { 
        color:#2563eb; 
        font-family:'Playfair Display', serif; 
        font-size:1.5rem; 
        margin-bottom:15px; 
        font-weight:600; 
    }

    /* ENHANCED ANIMATIONS */
    @keyframes gradientShift { 
        0% {background-position:0% 50%;} 
        50% {background-position:100% 50%;} 
        100% {background-position:0% 50%;} 
    }
    
    @keyframes fadeZoom { 
        0% {opacity:0; transform:scale(0.8) rotateX(-10deg);} 
        100% {opacity:1; transform:scale(1) rotateX(0);} 
    }
    
    @keyframes fadeUp { 
        0% {opacity:0; transform:translateY(30px);} 
        100% {opacity:1; transform:translateY(0);} 
    }
    
    @keyframes gentlePulse { 
        0%,100% {transform:scale(1);} 
        50% {transform:scale(1.05);} 
    }
    
    @keyframes float {
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-10px); }
    }
    
    @keyframes cardFloat {
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-5px); }
    }
    
    @keyframes glow {
        0% { text-shadow: 0 0 10px rgba(255,255,255,0.5); }
        100% { text-shadow: 0 0 20px rgba(255,255,255,0.8), 0 0 30px rgba(59, 130, 246, 0.5); }
    }
    
    @keyframes textShimmer {
        0% { background-position: -200px center; }
        100% { background-position: 200px center; }
    }
    
    @keyframes widthGrow {
        0% { width: 60px; }
        100% { width: 120px; }
    }
    
    @keyframes rotate {
        0% { transform: rotate(0deg); }
        100% { transform: rotate(360deg); }
    }
    
    @keyframes iconBounce {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-5px); }
    }
    
    @keyframes buttonPulse {
        0%, 100% { box-shadow: 0 5px 15px rgba(37, 99, 235, 0.2); }
        50% { box-shadow: 0 5px 20px rgba(37, 99, 235, 0.4); }
    }
    
    @keyframes successPop {
        0% { opacity: 0; transform: scale(0.8) rotateY(-20deg); }
        100% { opacity: 1; transform: scale(1) rotateY(0); }
    }
    
    @keyframes shimmer {
        0% { background-position: -200px 0; }
        100% { background-position: 200px 0; }
    }

    /* Responsive adjustments */
    @media (max-width: 768px) {
        .header { height: 220px; }
        .header-content h1 { font-size: 2.2rem; }
        .header-tagline { font-size: 1.1rem; }
        .emergency-number-container { flex-direction: column; text-align: center; }
        .emergency-number { font-size: 1.8rem; }
    }
</style>
</head>
<body>
<div class="container">
    <!-- HEADER -->
    <div class="header">
        <div class="header-content">
            <h1>Emergency Ambulance Services</h1>
            <p class="header-tagline">Immediate Medical Assistance • 24/7 Availability • Free Service</p>
        </div>
    </div>

    <!-- EMERGENCY CARD -->
    <div class="emergency-card">
        <h2 class="section-title">Emergency Contact Number</h2>
        <div class="emergency-number-container">
            <div class="number-icon"><i class="fas fa-phone-alt"></i></div>
            <div class="emergency-number"><span>108</span> - Ambulance Emergency</div>
        </div>
        <div class="contact-info"><i class="fas fa-info-circle" style="margin-right:8px;"></i>Call <strong>108</strong> for immediate ambulance service. The nearest hospital ambulance will be dispatched to your location within minutes.</div>
    </div>

    <!-- FORM CARD -->
    <div class="form-card">
        <h2 class="section-title">Request Ambulance</h2>
        <p style="color:#64748b; margin-bottom:30px; font-size:1.1rem;">Enter your address below. An ambulance will be dispatched immediately from the nearest hospital.</p>
        <form id="ambulanceForm" method="post" action="/citizen/emergency/ambulance">
            <div class="form-group">
                <label class="form-label" for="address"><i class="fas fa-map-marker-alt"></i> Complete Address</label>
                <textarea class="address-input" id="address" 
               name="address" placeholder="Enter your complete address including street, area, landmark..." required></textarea>
            </div>
            <button type="submit" class="submit-btn"><i class="fas fa-ambulance"></i> Request Ambulance</button>
        </form>
        <div class="success-message" id="successMessage">
            <div class="success-header"><i class="fas fa-check-circle"></i><h3>Ambulance Dispatched</h3></div>
            <div class="success-content">
                <p>An ambulance has been dispatched to your location.</p>
                <p><strong>Estimated arrival:</strong> 8-12 minutes</p>
                <p>Please keep the patient comfortable and phone accessible.</p>
                <div class="service-id">Service ID: <span id="serviceId">AMB-0456723</span></div>
            </div>
        </div>
    </div>

    <!-- FOOTER -->
    <div class="footer">
        <div class="footer-logo">Emergency Medical Services</div>
        <p style="margin-bottom:15px; color:#64748b;">24/7 Emergency Response • Free Service</p>
        <p style="font-size:0.95rem;"><i class="fas fa-phone" style="margin-right:8px; color:#ef4444;"></i>Emergency: 108 • <i class="fas fa-info-circle" style="margin-right:8px; color:#2563eb;"></i>Information: 104</p>
    </div>
</div>
</body>
</html>
